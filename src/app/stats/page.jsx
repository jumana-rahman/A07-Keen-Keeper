import React from 'react';

export const metadata = {
  title: "Keen Keeper | Stats",
};

const StatsPage = () => {
    return (
        <div>
            stats
        </div>
    );
};

export default StatsPage;