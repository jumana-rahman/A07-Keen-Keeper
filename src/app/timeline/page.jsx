import React from 'react';

export const metadata = {
  title: "Keen Keeper | Timeline",
};

const TimelinePage = () => {
    return (
        <div>
            Timeline
        </div>
    );
};

export default TimelinePage;